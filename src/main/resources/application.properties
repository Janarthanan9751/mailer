# Gmail API Configuration
# These properties are populated from environment variables at runtime
# For Render deployment, set these environment variables:
#   GMAIL_CLIENT_ID (automatically maps to gmail.client.id)
#   GMAIL_CLIENT_SECRET (automatically maps to gmail.client.secret)  
#   GMAIL_REFRESH_TOKEN

# Property definitions - values come from environment variables
gmail.client.id=
gmail.client.secret=
GMAIL_REFRESH_TOKEN=
